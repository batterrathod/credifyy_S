{
  "name": "moneyview-scraper",
  "version": "1.0.0",
  "description": "Moneyview IVR Scraper for Render",
  "main": "scraper.js",
  "scripts": {
    "start": "node scraper.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "puppeteer": "^24.1.1",
    "mysql2": "^3.11.0",
    "dotenv": "^16.4.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
